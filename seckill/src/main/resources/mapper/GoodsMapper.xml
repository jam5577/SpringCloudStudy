<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jam.mapper.GoodsMapper">

    <select id="findGoods" resultType="com.jam.vo.GoodsVO">
        SELECT g.id,
               g.goods_detail,
               g.goods_img,
               g.goods_name,
               g.goods_title,
               g.goods_price,
               g.goods_stock,
               sg.seckill_price,
               sg.stock_count,
               sg.start_date,
               sg.end_date
        FROM seckill.goods g LEFT JOIN seckill.sec_kill_goods sg ON g.id = sg.goods_id
    </select>
</mapper>
